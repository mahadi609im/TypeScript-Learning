type IsBoolean = boolean;

const isAdmin: IsBoolean = true;
const isUser: IsBoolean = false;

/**
 *  but interface a eta kora jabe naa 
 * karon ... 
 * interface IStudent {
    name: string;
    roll: number;
    classYear: string;
  }
* Eta ekta object, so interface a primitive type data use kora jabe naa ... 

 */
